export enum RouteAppPath {
  Root = "",
  Health = "health",
  Dashboard = "dashboard",
  User = "user",
}

export enum RouteUserPath {
  Users = "users",
  Permissions = "permissions",
  Roles = "roles",
  UserProfile = "user-profile",
}
